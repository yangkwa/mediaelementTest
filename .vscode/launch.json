{
    "configurations": [
        {
            "name": "Launch Microsoft Edge and open the Edge DevTools",
            "request": "launch",
            "type": "pwa-msedge",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}